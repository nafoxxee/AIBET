# 🔧 Исправление ошибки деплоя на Render

## ❌ **Проблема:**
Render ищет `single_service_main.py` в неправильном месте.

## ✅ **Решение:**

### 1. **Переместите файл в корень:**
Файл `single_service_main.py` должен быть в корне репозитория.

### 2. **Правильная структура:**
```
AIBET/
├── single_service_main.py  ← В КОРНЕ!
├── app/
├── cs2/
├── khl/
├── storage/
├── requirements.txt
└── .env
```

### 3. **Исправьте в GitHub:**
1. Переместите `single_service_main.py` в корень репозитория
2. Обновите репозиторий на GitHub

### 4. **Обновите настройки Render:**
- **Root Directory**: Оставьте пустым (корень репозитория)
- **Start Command**: `python single_service_main.py`

## 🚀 **Быстрое исправление:**

### В терминале:
```bash
# Перейдите в корень проекта
cd "c:\AI BET\aibet-analytics-platform"

# Переместите файл (если нужно)
# Файл уже в корне, просто обновите GitHub

# Обновите GitHub
git add .
git commit -m "Fix: Move single_service_main.py to root"
git push origin main
```

### На GitHub:
1. Убедитесь, что `single_service_main.py` в корне
2. Обновите репозиторий

### На Render:
1. **Root Directory**: Пустое
2. **Start Command**: `python single_service_main.py`
3. **Build Command**: `pip install -r requirements.txt`

## 📁 **Проверьте структуру:**
Файл `single_service_main.py` должен быть на том же уровне, что и:
- `requirements.txt`
- `app/`
- `cs2/`
- `khl/`

После исправления деплой должен сработать! 🚀

# AI BET AnТы senior backend + devops инженер.

У нас есть реальный деплой на Render.
Сборка проходит успешно, но запуск падает.

Текущая ошибка:

ModuleNotFoundError: No module named 'aiohttp_cors'

Это означает, что библиотека используется в коде,
но отсутствует в requirements.txt.

Твоя задача — НЕ ПЕРЕПИСЫВАТЬ ПРОЕКТ,
НЕ ДОБАВЛЯТЬ ЛИШНИЕ МОДУЛИ,
НЕ МЕНЯТЬ АРХИТЕКТУРУ.

Нужно сделать строго следующее:

1. Найди все места в проекте, где используется:
   import aiohttp_cors

2. Добавь зависимость:
   aiohttp-cors
   в requirements.txt

3. Убедись, что requirements.txt содержит корректное имя пакета:
   aiohttp-cors (через дефис)

4. Проверь, что после установки проект запускается без ошибок импорта.

5. Проверь файл main.py:
   — приложение обязано слушать порт из переменной окружения PORT
   — Render Web Service без этого не запускается

   Обязательно должно быть:

   PORT = int(os.environ.get("PORT", 10000))
   uvicorn.run(app, host="0.0.0.0", port=PORT)

6. Если используется FastAPI + bot одновременно —
   бот должен запускаться через asyncio.create_task,
   чтобы не блокировать uvicorn.

7. После исправлений:
   — сделай commit
   — push в GitHub
   — дождись автодеплоя Render

8. После завершения выведи отчёт:
   ✔ какие файлы изменены
   ✔ что именно исправлено
   ✔ почему ошибка исчезнет

Никаких рассуждений.
Никаких новых функций.
Никакой рефакторизации.

Только устранение ошибки деплоя.

Это приоритет №1.alytics Platform - Fixed Requirements
# Core dependencies
aiohttp>=3.8.0
aiosqlite>=0.17.0
aiogram>=3.0.0

# Web server for health checks
fastapi>=0.104.0
uvicorn[standard]>=0.24.0

# Data processing
pandas>=1.5.0
numpy>=1.21.0
scikit-learn>=1.1.0
joblib>=1.1.0

# Web scraping
beautifulsoup4>=4.11.0
lxml>=4.9.0
requests>=2.28.0

# Configuration and utilities
python-dotenv>=1.0.0
pydantic>=1.9.0

# Date/time handling
python-dateutil>=2.8.0

# Optional: For enhanced web scraping
selenium>=4.15.0

# Optional: For data visualization
matplotlib>=3.5.0
seaborn>=0.11.0

# Security
cryptography>=3.4.0

# HTTP client with better features
httpx>=0.24.0

# For better async context management
aiofiles>=0.8.0

# For better error handling
tenacity>=8.0.0

# For caching
cachetools>=5.0.0

# For rate limiting
async-limiter>=0.1.1

# 🚀 AIBET Production Deployment Guide

## ✅ **Статус: ГОТОВ К ДЕПЛОЮ**

### **📋 Что реализовано:**

#### **🔍 Парсеры реальных данных:**
- **CS2 Parser**: HLTV.org + 5 API зеркал + fallback источники
- **KHL Parser**: livesport.com + khl.ru + sofascore + flashscore
- **Odds Parser**: BetBoom, Winline, Fonbet с обновлением каждые 5 минут

#### **🤖 ML Pipeline:**
- **3 модели**: RandomForest, GradientBoosting, LogisticRegression
- **20+ фичей**: Win rate, форма, моментум, H2H, консистентность
- **Обучение**: Последние 100 матчей команд
- **Прогнозы**: Confidence ≥ 70% для сигналов

#### **📡 API Эндпоинты:**
```
GET /api/health          - Health check
GET /api/matches         - Все матчи CS2/KHL
GET /api/matches/refresh - Обновление матчей
GET /api/odds           - Коэффициенты букмекеров
GET /api/odds/average   - Средние коэффициенты
GET /api/ml_predictions - ML прогнозы
POST /api/ml_models/train - Обучение моделей
GET /api/signals        - Авто-сигналы
GET /api/stats/teams/{team} - Статистика команды
```

#### **🤖 Telegram Bot Команды:**
```
/start     - Главное меню с кнопкой Mini App
/live      - Live матчи CS2 + KHL
/signals   - Точные сигналы (≤10/день)
/analysis  - Анализ матча по ID
/stats     - Статистика команды
/odds      - Коэффициенты матча
/miniapp   - Открыть Mini App
/admin     - Админ панель
```

---

## 🔄 **Процесс деплоя на Render:**

### **1. Файлы готовы:**
- ✅ `render.yaml` - Конфигурация двух сервисов
- ✅ `main_production.py` - Production запуск
- ✅ `Dockerfile.web` - Для API сервера
- ✅ `Dockerfile.bot` - Для Telegram бота
- ✅ `requirements_full.txt` - Все зависимости

### **2. Render Сервисы:**
- **aibet-mini-app** (порт 10000): API + Mini App
- **aibot-telegram-bot** (порт 10001): Telegram бот

### **3. Автоматический деплой:**
```bash
git add .
git commit -m "🚀 Production ready with real data and ML"
git push origin main
```

Render автоматически соберет и задеплоит оба сервиса.

---

## 🧪 **Проверка после деплоя:**

### **API Проверка:**
```bash
# Health check
curl https://aibet-mini-app.onrender.com/api/health

# Получить матчи
curl https://aibet-mini-app.onrender.com/api/matches

# Получить прогнозы
curl https://aibet-mini-app.onrender.com/api/ml_predictions

# Получить сигналы
curl https://aibet-mini-app.onrender.com/api/signals
```

### **Telegram Bot Проверка:**
```bash
# Health check
curl https://aibot-telegram-bot.onrender.com/health
```

### **Логи на Render:**
1. Зайти в Render Dashboard
2. Открыть сервис aibet-mini-app
3. Проверить Logs вкладку
4. Открыть сервис aibot-telegram-bot
5. Проверить Logs вкладку

---

## 📊 **Ожидаемые результаты:**

### **API Должен возвращать:**
- ✅ Реальные матчи CS2 и KHL
- ✅ Актуальные коэффициенты
- ✅ ML прогнозы с confidence
- ✅ Авто-сигналы (confidence ≥ 70%)

### **Telegram Bot Должен:**
- ✅ Показывать live матчи
- ✅ Отправлять сигналы
- ✅ Анализировать матчи по ID
- ✅ Показывать статистику команд

---

## 🛠️ **Troubleshooting:**

### **Если API не работает:**
1. Проверить логи в Render
2. Убедиться что `SERVICE_TYPE=web`
3. Проверить порт 10000

### **Если бот не работает:**
1. Проверить `TELEGRAM_BOT_TOKEN`
2. Убедиться что `SERVICE_TYPE=bot`
3. Проверить порт 10001

### **Если нет данных:**
1. Подождать 5 минут для первого обновления
2. Проверить фоновые задачи в логах
3. Проверить доступность источников данных

---

## 🎯 **Key Features:**

### **✅ Реальные данные:**
- CS2: HLTV.org + API зеркала
- KHL: livesport.com + fallback
- Коэффициенты: 3 букмекера

### **✅ ML Аналитика:**
- 3 модели с cross-validation
- 20+ фичей из 100 матчей
- Confidence ≥ 70% для сигналов

### **✅ Автоматизация:**
- Обновление матчей каждые 5 минут
- Обновление коэффициентов каждые 5 минут
- Генерация сигналов каждый час

### **✅ Надежность:**
- Retry механизмы
- Fallback источники
- Health checks
- Комплексное логирование

---

## 🚀 **Ready for Production!**

Проект полностью готов к продакшен использованию:
- 🎯 Реальные данные без фейков
- 🤖 ML прогнозы с высокой точностью
- 📡 Полнофункциональный API
- 🤖 Работающий Telegram бот
- 🛡️ Надежность и мониторинг

**Деплой через git push в main ветку!** 🎉

# AI BET Telegram Mini App

## 🎯 Обзор

Полноценная Telegram Mini App для аналитики спортивных рынков с интеграцией ML-системы и real-time обновлениями.

## 🏗️ Архитектура

```
AI BET/
├── frontend/                 # Telegram Mini App
│   ├── index.html           # Основной интерфейс
│   ├── styles.css           # Стили с темной темой
│   └── script.js            # JavaScript с Telegram WebApp SDK
├── aibet-analytics-platform/ # Backend система
│   ├── app/
│   │   ├── api.py          # API сервер для Mini App
│   │   ├── main.py         # Telegram бот
│   │   └── config.py       # Конфигурация
│   ├── cs2/                # Модуль CS2 аналитики
│   ├── khl/                # Модуль КХЛ аналитики
│   └── storage/            # База данных
└── README_MINI_APP.md      # Этот файл
```

## 🚀 Быстрый запуск

### 1. Установка зависимостей

```bash
cd aibet-analytics-platform
pip install -r requirements.txt
```

### 2. Настройка окружения

```bash
# Копировать шаблон конфигурации
cp .env.example .env

# Редактировать .env файл
nano .env
```

**Обязательные переменные:**
- `TELEGRAM_BOT_TOKEN` - токен Telegram бота
- `ADMIN_TELEGRAM_IDS` - ID администраторов через запятую
- `CS2_CHANNEL_ID` - канал для CS2 аналитики
- `KHL_CHANNEL_ID` - канал для КХЛ аналитики

### 3. Создание Telegram бота

1. Найти [@BotFather](https://t.me/botfather) в Telegram
2. Отправить команду `/newbot`
3. Следовать инструкциям для создания бота
4. Получить токен и добавить в `.env`

### 4. Создание Telegram каналов

1. Создать каналы:
   - CS2: `@aibetcsgo`
   - КХЛ: `@aibetkhl`
2. Сделать бота администратором каналов
3. Получить ID каналов и добавить в `.env`

### 5. Запуск приложения

```bash
# Запуск полного приложения
python main.py

# Или только API сервера
python -m app.api
```

## 📱 Mini App функционал

### Основные экраны

- **Главная панель** - навигация по разделам
- **CS2 аналитика** - матчи и сценарии CS2
- **КХЛ аналитика** - матчи и сценарии КХЛ
- **Live матчи** - трансляции в реальном времени
- **Предматч** - будущие матчи с фильтрами
- **История** - статистика сигналов
- **Статистика** - детальная аналитика
- **Confidence** - рейтинги уверенности
- **Статус** - состояние системы
- **Админ-панель** - управление системой

### Функции Mini App

- **Real-time обновления** - WebSocket соединение
- **Адаптивный дизайн** - под любые экраны
- **Тёмная тема** - автоматическое определение
- **Фильтры и поиск** - удобная навигация
- **Интерактивные карточки** - детальная информация
- **Поделиться** - отправка в Telegram
- **Push уведомления** - через бота

## 🔐 Авторизация

Mini App использует Telegram WebApp SDK для авторизации:

1. Пользователь открывает Mini App через бота
2. Telegram передает данные пользователя
3. Backend проверяет подпись
4. Предоставляется доступ к функциям

### Админские права

Администраторы определяются через `ADMIN_TELEGRAM_IDS` в `.env`:

- Доступ к админ-панели
- Управление анализом
- Перезапуск систем
- Просмотр логов

## 🌐 API эндпоинты

### Аутентификация
- `POST /api/auth/check-admin` - проверка админских прав

### CS2 аналитика
- `GET /api/cs2/matches` - матчи CS2
- `GET /api/cs2/match/{id}` - детали матча

### КХЛ аналитика
- `GET /api/khl/matches` - матчи КХЛ
- `GET /api/khl/match/{id}` - детали матча

### Live и предматчи
- `GET /api/live/matches` - live матчи
- `GET /api/prematch/matches` - предматчи

### Статистика
- `GET /api/history/signals` - история сигналов
- `GET /api/stats/{sport}` - статистика
- `GET /api/confidence/ratings` - рейтинги уверенности

### Система
- `GET /api/system/status` - статус системы
- `POST /api/admin/action` - админские действия

### WebSocket
- `WS /api/ws` - real-time обновления

## 📊 Интеграция с Backend

Mini App интегрирована с существующими модулями:

### CS2 модуль
```python
from cs2.analysis.scenarios import CS2ScenarioAnalyzer
from cs2.ml.predictor import CS2Predictor
```

### КХЛ модуль
```python
from khl.analysis.scenarios import KHLScenarioAnalyzer
from khl.ml.predictor import KHLPredictor
```

### База данных
```python
from storage.database import DatabaseManager
```

## 🎨 Кастомизация

### Изменение цветовой схемы

В `styles.css` измените CSS переменные:

```css
:root {
    --primary-color: #0088cc;
    --success-color: #28a745;
    --warning-color: #ffc107;
    --danger-color: #dc3545;
}
```

### Добавление новых экранов

1. Добавить HTML в `index.html`
2. Добавить стили в `styles.css`
3. Добавить логику в `script.js`
4. Добавить API эндпоинт в `app/api.py`

### Настройка фильтров

В `script.js` изменить обработчики фильтров:

```javascript
document.addEventListener('change', (e) => {
    if (e.target.id.includes('filter')) {
        // Применить фильтры
        loadScreenData(currentScreen);
    }
});
```

## 🔧 Разработка

### Локальное тестирование

Для тестирования Mini App в браузере:

1. Запустить API сервер
2. Открыть `frontend/index.html`
3. Использовать тестовые данные пользователя

### Отладка

Включить отладку в `.env`:

```env
DEBUG=true
LOG_LEVEL=DEBUG
```

### Тестирование WebSocket

```javascript
// В консоли браузера
const ws = new WebSocket('ws://localhost:8080/api/ws');
ws.onmessage = (event) => console.log(JSON.parse(event.data));
```

## 📈 Мониторинг

### Логи приложения

```bash
tail -f logs/app.log
```

### Статус API

```bash
curl http://localhost:8080/api/system/status
```

### WebSocket соединения

Проверить активные подключения в админ-панели.

## 🚀 Деплоймент

### Требования

- Python 3.8+
- 24/7 сервер для работы
- Стабильное интернет соединение
- 2GB+ RAM

### Docker (опционально)

```dockerfile
FROM python:3.9-slim

WORKDIR /app
COPY requirements.txt .
RUN pip install -r requirements.txt

COPY . .
CMD ["python", "main.py"]
```

### Reverse Proxy (Nginx)

```nginx
server {
    listen 80;
    server_name your-domain.com;
    
    location /api/ {
        proxy_pass http://localhost:8080;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
    }
    
    location / {
        root /path/to/frontend;
        try_files $uri $uri/ /index.html;
    }
}
```

## 🛡️ Безопасность

### Защита API

- Проверка Telegram подписи
- Rate limiting
- CORS настройки
- Валидация данных

### Безопасность Mini App

- Только авторизованные пользователи
- Проверка админских прав
- Защита от XSS
- Безопасная передача данных

## 📝 Лицензия

Проект предоставляется для образовательных целей. Ответственность за соблюдение законов о спортивных ставках лежит на пользователе.

## 🤝 Поддержка

Для вопросов и предложений:
1. Проверить логи
2. Изучить документацию
3. Создать issue в репозитории

---

**AI BET Analytics Platform** - профессиональная аналитика спортивных рынков в Telegram.

# AI BET Analytics Platform - Render Deployment Guide

## 🚀 Деплой на Render

Проект разделен на два независимых веб-сервиса для стабильной работы на Render.

## 📋 Сервисы

### 1. Web Service (Mini App + API)
- **Скрипт запуска**: `main_web.py`
- **Порт**: Автоматически из `PORT` (Render)
- **URL**: Ваш домен на Render
- **Функции**: Mini App интерфейс, API endpoints

### 2. Bot Service (Telegram Bot + Scheduler)
- **Скрипт запуска**: `main_bot.py`
- **Порт**: Не требуется (работает через webhook)
- **Функции**: Telegram бот, планировщик задач

## 🔧 Настройка переменных окружения

### Для Web Service:
```
PORT=10000
```

### Для Bot Service:
```
TELEGRAM_BOT_TOKEN=your_bot_token_here
ADMIN_TELEGRAM_IDS=123456789,987654321
WEBHOOK_URL=https://your-bot-service.onrender.com
```

## 📦 Установка зависимостей
```bash
pip install -r requirements.txt
```

## 🏃‍♂️ Запуск локально

### Web Service:
```bash
python main_web.py
```
Доступ: http://localhost:10000

### Bot Service:
```bash
python main_bot.py
```

## 🌐 API Endpoints (Web Service)

### Основные:
- `GET /` - Mini App интерфейс
- `GET /api/health` - Проверка здоровья
- `GET /api/stats` - Статистика
- `GET /api/matches` - Матчи
- `GET /api/signals` - Сигналы
- `GET /ping` - Pinger для Render

### Документация:
- `GET /docs` - Swagger UI
- `GET /redoc` - ReDoc

## 🤖 Telegram Bot Commands

- `/start` - Главное меню
- Кнопки: Статистика, CS2 матчи, КХЛ матчи, Сигналы, Настройки

## 📊 Структура проекта

```
AI BET/
├── main_web.py          # Web Service (FastAPI)
├── main_bot.py          # Bot Service (aiogram)
├── requirements.txt     # Зависимости
├── logs/               # Логи (создается автоматически)
└── README.md           # Этот файл
```

## 🔄 Автоматический перезапуск

Оба сервиса имеют встроенную обработку ошибок и автоматический перезапуск при сбоях.

## 📝 Логирование

- Файл: `logs/app.log`
- Консоль: stdout
- Уровень: INFO

## 🛠️ Отладка

Проверьте логи в `logs/app.log` для диагностики проблем.

## 🎯 Render Configuration

### Web Service:
- **Type**: Web Service
- **Runtime**: Python 3
- **Build Command**: `pip install -r requirements.txt`
- **Start Command**: `python main_web.py`

### Bot Service:
- **Type**: Web Service  
- **Runtime**: Python 3
- **Build Command**: `pip install -r requirements.txt`
- **Start Command**: `python main_bot.py`

## 📈 Мониторинг

- Health checks: `/api/health` и `/ping`
- Автоматическое логирование всех операций
- Обработка сетевых ошибок

## 🚨 Важно

1. Убедитесь, что `TELEGRAM_BOT_TOKEN` правильно настроен
2. Web Service должен быть доступен по порту из `PORT`
3. Bot Service не требует открытого порта
4. Логи автоматически создаются в директории `logs/`

## 🎉 Готово к деплою!

Проект полностью готов к деплою на Render с минимальной конфигурацией.
